version: 1
frontend:
  phases:
    build:
      commands:
        - cd gigfinder-app
        - npm install --force
        - npm run build
  artifacts:
    baseDirectory: gigfinder-app/build
    files:
      - '**/*'
  cache:
    paths:
      - gigfinder-app/node_modules/**/*
