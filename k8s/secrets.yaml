# TEMPLATE ONLY — do not commit real values!
# Apply secrets via: kubectl create secret generic whatsthecraic-secrets \
#   --from-literal=DB_HOST=<rds-endpoint> \
#   --from-literal=DB_USER=admin \
#   --from-literal=DB_PASSWORD=<password> \
#   --from-literal=JWT_SECRET=<jwt-secret> \
#   --from-literal=TICKETMASTER_API_KEY=<key> \
#   --from-literal=BANDSINTOWN_APP_ID=<id> \
#   --from-literal=SPOTIFY_CLIENT_ID=<id> \
#   --from-literal=SPOTIFY_CLIENT_SECRET=<secret> \
#   -n whatsthecraic
apiVersion: v1
kind: Secret
metadata:
  name: whatsthecraic-secrets
  namespace: whatsthecraic
  labels:
    app.kubernetes.io/part-of: whatsthecraic
type: Opaque
stringData:
  DB_HOST: "CHANGE_ME"
  DB_USER: "CHANGE_ME"
  DB_PASSWORD: "CHANGE_ME"
  JWT_SECRET: "CHANGE_ME"
  TICKETMASTER_API_KEY: "CHANGE_ME"
  BANDSINTOWN_APP_ID: "CHANGE_ME"
  SPOTIFY_CLIENT_ID: "CHANGE_ME"
  SPOTIFY_CLIENT_SECRET: "CHANGE_ME"
  SPOTIFY_REDIRECT_URI: "https://auth.whatsthecraic.run.place/auth/spotify/callback"
  SPOTIFY_SCOPES: "user-top-read user-follow-read user-library-read user-read-email"
  EVENTBRITE_API_TOKEN: ""
  APIFY_TOKEN: ""
