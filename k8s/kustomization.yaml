apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: whatsthecraic

resources:
  - namespace.yaml
  - configmap.yaml
  # secrets.yaml is a template — apply real secrets via kubectl create secret
  - auth-service.yaml
  - events-service.yaml
  - dj-service.yaml
  - venue-service.yaml
  - aggregator.yaml
  - ml-service.yaml
  - ingress.yaml
  - hpa.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/part-of: whatsthecraic
